## python fastAPI bookstore
#### create Virtual Environment with python venv:
```sh
python -m vevn venv
```
activate venv:
```sh
souce venv/bin/activate
```
#### install dependicies using `pip` from requirement.txt
#### run project
```sh
uvicorn books:app --reload
```
